<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="series"
            type="com.doiliomatsinhe.mymovies.model.person.PersonTvCast" />

        <variable
            name="clicklistener"
            type="com.doiliomatsinhe.mymovies.adapter.person.PersonSeriesClickListener" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="193dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        android:onClick="@{() -> clicklistener.onClick(series)}"
        app:cardElevation="2dp"
        app:cardPreventCornerOverlap="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/image_series_cover"
                android:layout_width="0dp"
                android:layout_height="290dp"
                android:contentDescription="@string/series_poster"
                android:scaleType="centerCrop"
                app:itemImage="@{series}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:src="@drawable/tv_poster" />

            <TextView
                android:id="@+id/text_series_character"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="3"
                android:padding="8dp"
                android:textAlignment="center"
                android:textSize="16sp"
                app:characterName="@{series}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/image_series_cover"
                tools:text="as\nRick Sanchez" />


        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>


</layout>